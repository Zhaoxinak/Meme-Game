# 《脑洞军团大乱斗·沙雕自动对战》游戏企划 v2（机制扩充版）

> 定位不变：**纯自动对战 + 轻量"战术指挥"参与感**。
> v2 在 v1 的基础上，把"战场深度"从 4 个模块扩到 **15 个机制**，全部基于现有代码底座
> （2.5D 斜投影 `PROJ/applyWorld`、`COMMANDS` + `castCmd` 指令框架、`aoeAt` 范围结算、
> `BRANCHES` 分支三元环、`PERKS` 战术卡、`makeUnit` 单位模型、`compact` 原地压缩）。
>
> **阅读方式**：每个机制都写成「规则 → 数值草案 → 代码落点 → 演出 → 风险」五段式，
> 可以直接按模块实现。数值未经 playtest 的一律标 `[PLACEHOLDER]`。

---

## 0. 设计原则（v1 五条 + v2 新增三条）

| # | 原则 | 说明 | 反例 |
|---|---|---|---|
| 1 | 自动为主 | 单位自动寻敌/攻击/走位，玩家只"指挥"不"操作" | 变成需手动走位的 RTS |
| 2 | **对称基线** | 敌我共用同一套状态/地形/指令/事件。AI 也会踩泉、被冰封、用指挥点 | 玩家指令凭空更强 |
| 3 | 一点点参与感 | 指令有 CD + 指挥点上限，防无脑乱按 | 无限连发秒全场 |
| 4 | 沙雕演出 | 演出优先于写实，鼓励连锁/翻盘/翻车名场面 | 严肃军事模拟 |
| 5 | 性能不回退 | 新状态走**扁平数值字段**（非对象）、单遍结算、复用 `compact` | 每帧 filter / 对象分配 |
| 6 | **不破坏三元环** 🆕 | 近战→骑兵→远程→近战的克制环是平衡地基。新单位一律走"特殊编制"（中性 ×1），**不进克制环** | 加第 4 个基础兵种，三元环直接崩 |
| 7 | **模块可关** 🆕 | 每个机制一个 `FLAGS` 开关 + 独立 diff。出问题能单独关掉回滚 | 一坨耦合，炸了只能整局回退 |
| 8 | **不惩罚落后者** 🆕 | 翻盘机制给的是"双向对称 buff"，不给劣势方 debuff。避免雪崩 | 劣势方再减速 → 一边倒的垃圾时间 |

### 0.1 对称自检清单（每个新机制上线前必答）

1. 敌人能不能用？（不能 → 违反原则 2，要么给 AI 也实现，要么砍掉）
2. 它会不会让某一条线/某张卡变成唯一解？（会 → 收敛数值）
3. 它加的是**抉择**还是**白赚**？（白赚 → 玩家随机选也能赢，抉择就失去意义）
4. 最坏情况下会不会刷屏/卡顿？（会 → 加节流或对象池上限）
5. 关掉它，游戏还能正常跑吗？（不能 → 说明耦合太深，重做）

---

## 1. 【地基】状态效果系统 + 状态反应表 ★最高优先级

### 1.1 为什么是地基
落雷、新指令、新兵种、天候、物件……全都要"往单位身上挂点什么"。没有状态系统，
每个新内容都是一次性的；有了它，内容量是**乘**的。

### 1.2 数据结构（性能优先：扁平数值，不用对象）

```js
// makeUnit 里新增（全为 number / 0，无对象分配）
burnT:0, burnDps:0,   // 燃烧：剩余秒 / 每秒伤害
slowT:0, slowMul:1,   // 减速
stunT:0,              // 眩晕（复用 knocked 的演出位）
poisT:0, poisDps:0,   // 中毒
wetT:0,               // 湿润（本身无害，是反应催化剂）
shield:0,             // 护盾值（吸收伤害）
```

**结算位置**：`update()` 的单位循环里，紧跟 `u.buffT` 那段之后（约 L1008 附近），
单遍 `if` 分支，不新增循环、不新增数组。

| 状态 | 效果 | 来源（v2 全表） |
|---|---|---|
| 燃烧 burn | `hp -= burnDps*dt`，每 0.4s 冒一次紫火粒子 | 火墙、法师、落雷升级、炸药桶 |
| 减速 slow | 移速 ×slowMul | 冰封、泥沼、蛛网 |
| 眩晕 stun | 停止行动（`state="stun"`，复用 knocked 演出） | 冰封、碎冰反应、震地 |
| 中毒 poison | 持续掉血，**可被净化清除**（区别于燃烧） | 毒沼、特殊编制·毒师 |
| 湿润 wet | 无直接效果，专职做反应催化剂 | 泥沼、雨天、水桶 |
| 护盾 shield | 先扣盾再扣血；**破盾触发碎盾击退** | 治疗泉、护盾编制、治疗波溢出 |

### 1.3 状态反应表（本机制最值钱的部分）🆕

两种状态同时存在于一个单位身上 → 立刻触发反应，**双方状态同时清除**。
这是把 6 种状态炸成 15 种组合的花小钱办大事。

| 组合 | 反应名 | 效果 | 演出 |
|---|---|---|---|
| 燃烧 + 冰封 | 蒸汽爆 | 以自身为中心 `aoeAt(r=70, atk*1.2)`，附 `wet 2s` | 白雾环 + 白屏闪 |
| 湿润 + 冰封 | 深度冻结 | `stun 1.2s`（比普通冰封的 0.4s 长 3 倍） | 冰块包裹（drawUnit 加冰壳描边） |
| 湿润 + 落雷/闪电链 | 导电 | 该次伤害 ×1.5，且范围 +30% | 蓝色电弧连到附近 2 个湿单位 |
| 燃烧 + 中毒 | 毒爆 | `aoeAt(r=90)` 并给范围内所有敌人挂 `poison 3s` | 绿云扩散 |
| 护盾被打破 | 碎盾 | 自身 `knock 200`，周围 60 内友军 `slow 1s` | 玻璃碎裂粒子 |
| 眩晕 + 任意伤害 ≥atk*3 | 处决 | 直接 +50% 伤害（制造"控住→集火"的爽点） | 金色叉闪 + 顿帧 |

> **设计意图**：反应表让"先冰封再落雷"这种操作有**明确回报**，
> 玩家的指挥从"哪个亮了点哪个"变成"读战场状态、排技能顺序"。指挥深度 ×3，代码量 +80 行。

### 1.4 代码落点
- `makeUnit` (L691)：加 8 个字段
- `update()` (L1001 循环内)：单遍结算
- `damageUnit` (L784)：护盾吸收 + 破盾 + 处决判定
- 新增 `applyStatus(u, kind, dur, power)` / `reactStatus(u, kind)` 两个函数
- `drawUnit` (L1579)：状态描边（燃烧=橙红、冰冻=冰蓝、中毒=绿、护盾=金色环）

---

## 2. 战场物件层 `G.props`（吃透 2.5D 地图）

把"地面"从背景变成可交互战场。所有物件 `G.props=[]`，`{kind,x,y,r,hp,side?}`。

| 物件 | 机制 | 对称性 | 演出 |
|---|---|---|---|
| **炸药桶** ★ | 受任意伤害即爆（无差别，伤敌+友），可**连锁** | 天然对称 | 木桶 + 引信闪烁 |
| 泥沼 | 进入 `slow 0.6` + 挂 `wet 2s`（喂反应表） | 双向 | 深色贴花 + 气泡 |
| 治疗泉 | 站上去每秒回 2% 最大 HP；**谁站谁受益**，敌我通用 | 双向 | 光柱 + 上升粒子 |
| 石柱（隘口） | 静态障碍，软碰撞绕开（复用同阵营排斥逻辑） | 双向 | 与单位同一 y 排序 |
| **补给箱** 🆕 | 击破掉落：随机给破箱方 2 CP / 全体回 8% HP / 全体 `burn 3s`(陷阱箱 20%) | 双向 | 木箱 + 开箱粒子 |
| **弹射板** 🆕 | 踩上去的单位被弹向敌方方向 260px，落地震晕 0.3s（可能直接弹进敌阵送死） | 双向（友军也会被弹） | 弹簧压缩 + boing 音效 |

### 2.1 每轮随机布局
`startRound` 里调 `rollProps()`：从预设 6 套布局模板里抽 1 套 + 随机抖动 ±40px。
**为什么用模板而不是纯随机**：纯随机会出现"桶全堆在一边"的残局，模板能保证每套都打得起来。

### 2.2 代码落点
- `resetGame` (L650)：`props: []`
- `startRound` (L1255)：`rollProps()`
- `aoeAt` (L965) / `damageUnit` (L784)：命中时检测桶/箱 → `explodeProp()`
- `update()` (L1001)：泥沼/治疗泉/弹射板的范围检测（**每 0.25s 检测一次**，用 `u.propT` 节流，别每帧）
- `draw()` (L1466)：props 推进 `items` 参与 y 排序 → 免费获得透视与遮挡
- 绘制统一走 `withWorld(x,y,fn)`（L1464）

---

## 3. 指挥点 CP + 指令连锁（把"参与感"做成真系统）🆕

### 3.1 为什么需要 CP
现在只有 CD，等于"到点就按"，没有取舍。加 CP 之后，玩家每场要做**资源规划**：
这发落雷是现在砸后排，还是攒着配合冰封打碎冰？

```
上限 10 | 开局 4 | 每秒回 0.28 | 每次击杀 +0.25 | 占领治疗泉 +1/5s
```

| 指令 | CP | 说明 |
|---|---|---|
| 冲锋号 rally | 2 | 便宜、高频、滚雪球 |
| 嘲讽诱饵 decoy | 2 | 便宜，用来骗走骑兵 |
| 落雷 strike | 3 | 基准线 |
| 治疗波 heal | 3 | 基准线 |
| 补给空投 supply | 3 | 放一个临时治疗泉 |
| 冰封术 freeze | 3 | 便宜的控场（要靠反应赚回来） |
| 火墙 firewall | 4 | 贵，但持续伤害 + 喂燃烧反应 |
| 闪电链 chain | 4 | 贵，但打湿身敌人是核弹 |

### 3.2 指令连锁 Combo（3 秒窗口）

**3 秒内连放两条指令 → 按"系"的组合触发连锁**：

| 顺序 | 连锁名 | 效果 |
|---|---|---|
| 控场 → 进攻 | 趁虚而入 | 进攻指令伤害 ×1.35 |
| 辅助 → 进攻 | 养精蓄锐 | 该进攻指令 CD -2s，返还 1 CP |
| 进攻 → 进攻 | 疾风骤雨 | 第二发范围 +20%、CP -1 |
| 控场 → 控场 | 连环控制 | 第二发持续时间 +50% |

> 这就是给"手速党"和"脑党"的差异空间：**会排顺序的玩家，同样的 CP 打出 1.35 倍效果**。
> 而且它不破坏对称——因为敌方指挥官 AI 也会排顺序（见 §4.2）。

### 3.3 代码落点
- `resetGame`：`cp: {p:4, e:4, max:10}`
- `castCmd` (L413)：扣 CP、写 `G.cmd.last = {key, sys, t}`
- `update()`：`G.cp.p/e += 0.28*dt`（对称回复）、击杀回调加 CP
- `updateHud` (L2286)：CP 条（10 格能量条，复用现有 `.cd` 条的 CSS 思路）
- `refreshCmdUI` (L2419)：CP 不够时按钮置灰

---

## 4. 战术指挥扩到 8 指令 + **敌方指挥官 AI** ★对称关键

### 4.1 指令表（含 v1 的落雷/治疗波）

| 指令 | 系 | CP | CD | 机制 | 演出通道 |
|---|---|---|---|---|---|
| 落雷 strike | 进攻 | 3 | 7s | 已有 | `warn→beam` |
| 治疗波 heal | 辅助 | 3 | 12s | 已有（改：溢出治疗转护盾） | `ring` 绿 |
| **冰封术 freeze** | 控场 | 3 | 9s | 范围 `slow 2s` + `stun 0.4s` | 落冰读条 |
| **火墙 firewall** | 进攻 | 4 | 14s | 地面持续 3.5s 燃烧带，挂 burn | 持久 `effect` 带 |
| **冲锋号 rally** | 辅助 | 2 | 10s | 我方 `attackT*0.5` + 移速 +30%（2s） | 金光（同 cheer） |
| **嘲讽诱饵 decoy** | 控场 | 2 | 11s | 生成假单位（`decoy:true`，无攻击、嘲讽权重 ×5），4s 后消失 | `makeUnit` + 半透明 |
| **闪电链 chain** | 进攻 | 4 | 13s | 主目标 + 附近 2 敌连锁；**打湿身目标触发导电** | `bolt` 弹道弹射 |
| **补给空投 supply** | 辅助 | 3 | 15s | 战场放一个 15s 的临时治疗泉 | 降落伞箱子 |

> 诱饵的嘲讽权重：改 `findNearest` (L754) 为「距离 − decoy 权重×400」选最小，
> decoy 优先级最高。敌方单位同样会去打我方的诱饵 —— 双向对称。

### 4.2 敌方指挥官 AI（不做就是破坏对称）🆕

敌人也有一套 CP / CD / 指令池，由轻量 AI 消费：

| 触发条件 | AI 决策 |
|---|---|
| CP ≥ 4 且我方有 ≥3 单位聚在一起（半径 90） | 落雷砸最密处 |
| CP ≥ 3 且敌方（玩家）单位血量 <45% 的 ≥3 个 | 治疗波（AI 也会奶自己） |
| CP ≥ 3 且玩家骑兵正在冲锋 | 冰封拦截 |
| CP ≥ 2 且己方总兵力 <60% | 冲锋号 |
| 随机 8% / 每 6s | 随机一发（制造不可预测性） |

> **节奏控制**：AI 的决策检查**每 1.5 秒跑一次**（`G.enemyCmd.t` 计时），
> 不做每帧扫描；且不进行"完美预判"（不做 0 帧反应），保留玩家的时机优势。

### 4.3 代码落点
- `COMMANDS` (L401)：扩到 8 条，每条加 `sys`（"atk"/"sup"/"ctl"）与 `cp`
- `castCmd` (L413)：加 `casterSide` 参数，让敌我共用同一函数（**关键**：不写两份逻辑）
- 新增 `aiCommander(dt)`，在 `update()` 里被调用
- `buildCmdBar` (L2404)：8 个按钮（现在是 2 个，需要改 CSS grid 为 4×2 或滚动条）

---

## 5. 特殊编制（第 4 类单位，但**不进克制环**）🆕

### 5.1 为什么不做第 4 个基础兵种
三元环 `近战→骑兵→远程→近战` 是全部平衡的地基。加第 4 个基础系 = 环直接崩，
所有 `counterMul` / 分支专克 / AI 针对逻辑全部要重写。
**所以新单位走"特殊编制"：不参与克制环（打谁 ×1、被谁打 ×1），靠独特机制立足。**

### 5.2 编制池（每轮名额 2，随 Meta 天赋可 +1）

| 编制 | 机制 | 定位 | 反制方式 |
|---|---|---|---|
| 医师 medic | 攻击改为**治疗最残血友军**（heal = atk×1.6），自身极脆 | 续航 | 骑兵冲 / 落雷点 |
| 爆破手 sapper | 死亡时自爆 `aoeAt(r=110, atk×3)`（呼应炸药桶主题） | 反近战集群 | 远程点掉（自爆就浪费） |
| 法师 mage | 普攻挂 `burn 3s`；AoE 小火球 | 持续伤害 / 喂反应 | 近战贴脸 |
| 毒师 poisoner | 普攻挂 `poison`，可被净化 | 破治疗流 | 医师净化 |
| 鼓手 bard 🆕 | 光环 r=140：友军攻速 ×1.15（**唯一的全军常驻 buff**） | 团队核心，优先击杀目标 | 谁都想先杀他 |
| 斥候 scout 🆕 | 移速 ×1.4；开局 3 秒内给敌方全体 `slow 0.75`（先手） | 节奏 | 无关紧要，打不死也行 |

### 5.3 代码落点
- 新增 `SPECIALS` 表（结构对齐 `UNIT_DEFS` + `onAttack/onDeath/aura` 钩子）
- `makeUnit` (L691)：加 `spec` 字段
- `attack()` (L863)：`if (u.spec) SPECIALS[u.spec].onAttack(u, target)`
- `finishKill` (L802)：`onDeath` 钩子（爆破手自爆）
- `update()`：光环类每 0.5s 结算（`u.auraT` 节流）
- **名额在回合间与升级界面一起选**：`showUpgrade` (L1302) 加一行"编制"

---

## 6. 战前布阵（开局三选一）🆕

每轮开始前和升级一起做一次抉择：选阵型 → 决定 `spawnArmy` 的坐标分布 + 全军小特性。

| 阵型 | 布局 | 特性 | 被克 |
|---|---|---|---|
| 锋矢 wedge | 近战居中前压、远程两翼、骑兵最后 | 近战移速 +15% | 怕 AoE 扎堆 |
| 鹤翼 crane | 远程居中、近战护两翼、骑兵开局贴边绕后 | 远程射程 +10% | 怕骑兵直插中军 |
| 方圆 square | 密集方阵，三系混编 | 全体 HP +8%、移速 −10% | 怕火墙/落雷 |

> 成本极低（只改 `spawnArmy` L713 的坐标生成 + 一个 trait 表），
> 但每轮多一个决策点，且**与对面阵型产生博弈**（对面鹤翼 → 我方锋矢强冲中军）。

---

## 7. 老兵晋升（局内成长，正反馈）🆕

单位累计击杀 `u.kills`，当场晋升，**外观立刻变化**（开罗像素风最吃这套）：

| 击杀 | 称号 | 加成 | 外观 |
|---|---|---|---|
| 2 | 老兵 | 攻击 ×1.10 | 头盔加羽饰 |
| 4 | 精锐 | 攻击 ×1.20、HP ×1.15 | 头盔描金边 + 肩甲 |
| 6 | **战场传说** | 大招触发率 ×1.5、攻击 ×1.30 | 金色描边 + 头顶星标 + 拖尾粒子 |
| 传说阵亡 | — | 对方全军 `slow 1s`（哀兵） | 全屏大字「传说陨落」+ 慢动作 |

> 对称：**AI 单位同样计 kills、同样晋升**。玩家的优势只来自"让对的人拿到人头"
> （比如把残血留给已经 5 杀的单位 —— 但玩家不能微观操作，所以实际是随机 + 阵型带来的差异）。
> 这条主要是**演出价值**和"看着自己人一步步变强"的情感投入。

---

## 8. 士气系统（双向对称 buff，绝不惩罚落后者）🆕

由**双方当前兵力比**驱动，每 0.5s 结算一次（不要每帧）。

| 条件 | 阶段 | 效果（**双方同规则**） |
|---|---|---|
| 剩余兵力 ≤ 35% | **背水一战** | 伤害 ×1.25、移速 ×1.10，但受伤 ×1.15 |
| 兵力 ≥ 对手 3 倍 | **碾压推进** | 攻击间隔 −12%（加速结束垃圾时间） |
| 10 秒内损失 ≥ 40% | **阵线动摇** | 全军 `slow 0.85` 持续 3s（一次性，不叠加） |

> 关键：劣势方拿到的是**净 buff**（×1.25 伤害 vs ×1.15 受伤，还是赚的），
> 所以是"翻盘窗口"而不是"雪崩"。碾压推进那条是为了砍掉最后 10 秒的垃圾时间。

**代码落点**：`G.morale = {p:"normal", e:"normal", t:0}`；新增 `updateMorale(dt)`，
系数读进 `attack()` / `damageUnit`。

---

## 9. 天候时间轴（把"开局 MOD"升级成"会变的战场"）🆕

v1 的战场 MOD 只在开局生效一次，之后就忘了。v2 做成**每轮一条时间轴**：
`G.weather = { key, t, nextAt }`，每 18~22 秒轮转一次，全场无差别。

| 天候 | 效果 | 制造的名场面 |
|---|---|---|
| 雷暴 | 每 1.5s 随机落一发小雷（无差别，会点着炸药桶） | 全场乱雷 + 桶链核爆 |
| 沙暴 | 远程射程 −25%、远程命中飘 15% | 弓兵集体摸鱼 |
| 血月 | 大招触发率 ×2、大招 CD −30% | 满屏大招互轰 |
| 补给空投 | 中央落 3 个补给箱，先到先得 | 抢箱子混战 |
| 重力异常 | 击退 ×2.2（落雷 = 发射） | 全员飞天 |
| 镜面 | 双方属性短暂互换 5s（纯演出 + 数值对调，净效果趋近 0） | 一脸懵逼 |

**代码落点**：`update()` 里 `if (G.t >= G.weather.nextAt) rollWeather()`；
用 `banner()` (L1271) 播报；`revertWeather()` 保证开关对称（**最容易出 bug 的地方**：
改了属性一定要在 revert 里还原，写成一个 `{apply, revert}` 对）。

---

## 10. 占点与超时判定（让"站桩"不再是超时最优解）🆕

现在超时判定是 `endBattle(pa, ea, true)` 直接比兵力数 → 劣势方最优解是跑路躲着不打。
加占点后，逃跑 = 送分。

- 中场 1 个控制点（半径 110）。
- 站在点内的兵力多的一方累积占领进度（每秒 +10%，僵持时不涨）。
- 占领方每 5 秒：全军回 3% 最大 HP + 1 CP（喂 §3 的资源系统）。
- **超时判定改为**：先比占点进度 → 相同再比剩余兵力。
- AI 占点：在 `update()` 的移动分支里，若己方未占领该点且单位距点 < 320，
  目标点向控制点偏移（权重 0.35）。**双方同一规则 → 对称**。

---

## 11. 指挥官上场与光环（P3，可选）🆕

`COMMANDER` (L407) 现在只是个结算用的假对象。让它**真的上场**：

- 用 `makeUnit` 生成，标记 `isCommander:true`，HP 是普通单位 ×3，站在阵型最后排。
- 光环 r=150：友军伤害 +10%。
- **阵亡 → 全军士气崩溃**（受伤 +20%，5 秒）+ 全屏大字「主帅阵亡！」
- 双方各一个 → 对称。AI 的指挥官不会特别保护自己（AI 不做微观），
  所以谁先摸到对方后排谁赚 —— 这正好让"骑兵绕后"这个定位有了战略价值。

---

## 12. 三角阵羁绊（低优先级，纯奖励）🆕

场上若存在 melee / ranged / cavalry 各至少 1 个且**互相在 120px 内**，
三者各获得攻速 +8%。演出：三者之间画一条淡色三角连线。
每 0.5s 检测一次，只在变化时重算。

> 价值：奖励"阵型没被冲散"的一方，视觉上也能让玩家看出"我的阵还完整吗"。

---

## 13. Meta：指挥官天赋树（localStorage）🆕

- 存档：`localStorage["nd_legion_meta_v1"] = {exp, lv, points, spent:{}}`
- 每局结算按「胜/负 + 总击杀 + 轮次表现」给经验。
- 天赋**只影响"你多能指挥"，不改基础数值**（守住对称原则）：

| 天赋 | 每级 | 上限 |
|---|---|---|
| 军师 | 指挥点上限 +2 | 3 |
| 通讯 | 指令 CD −8% | 3 |
| 后勤 | 开局 CP +2 | 2 |
| 参谋部 | 特殊编制名额 +1 | 2 |
| 老兵营 | 晋升所需击杀 −1 | 2 |
| 战地记者 | 解锁"名场面回放"（局后自动保存 3 个高光片段的静态帧） | 1 |

> **红线**：不提供"我方全体 +X% 属性"类天赋。
> 一旦给，玩家练满后单局对称就废了，且新玩家体验被碾压。
> 真要给兵力，就同时给 AI 同等兵力（保持对称）——但那等于只是提高难度，没有获得感，所以不做。

---

## 14. 模式与终局

> ⚠️ **骨架级改造已迁出**：守城远征（主模式）/ 竞技场（现有 5 轮）/ 镜像试炼 三模式并存方案、
> 经济与城墙系统的完整数值，见 **`脑洞军团大乱斗_守城远征_改造企划.md`**。
> 本节只保留 v2 各模式的设计细节。

| 模式 | 说明 | 依赖 |
|---|---|---|
| 战役（现有） | 5 轮 + 分支 + 战术卡 | — |
| 无尽 | 回合无限，兵力指数增长，刷分榜（localStorage） | §7 §8 |
| Boss 轮 | 第 5 轮改成精英 Boss（超大单位 + 周期大招 + 3 阶段） | §1 状态、§11 |
| 绝地反击 | 开局我方只有对面的 40% 兵力，靠背水一战 + 指挥翻盘 | §8 |
| 镜像 | 敌我完全同阵容同升级，胜负 100% 由"指挥"决定 —— **终极对称模式，也是最好的平衡测试场** 🆕 | §3 §4.2 |

> **镜像模式的隐藏价值**：它是验证"数值是否对称"的最强工具。
> 如果镜像模式下胜率不是 50%，说明某处还有非对称的隐藏加成（历史上连杀 +30% 就是这么被抓出来的）。

---

## 15. 演出 Juice + 名场面导演 🆕

### 15.1 名场面导演（Director）
一个常驻检测器，`G.director = {cool: 0}`，冷却 6 秒防刷屏：

| 触发 | 演出 |
|---|---|
| 一次伤害事件 ≥3 杀 | 慢动作 0.35×（0.7s）+ 推近 + 大字「天灾！」 |
| 炸药桶连锁 ≥2 | 大字「开幕雷击！」+ `sfx("laugh")` 罐头笑声 |
| 背水一战反杀获胜 | 「让二追三！」+ 撒花（`victoryConfetti` 已有） |
| 传说单位阵亡 | 「传说陨落」+ 0.35× 慢放 |
| 单轮 0 伤亡团灭对面 | 「完·美·收·官」+ 金色彩带 |

**慢动作实现**：`frame()` (L2455) 已有 `timeScale` 变量（`G.hitStop` 用），
加一个 `G.slowmo` 计时器复用同一通道即可，零新架构。
**推近**：`applyWorld` (L1459) 加一个 `G.cam.zoom` 缩放 —— 中等成本，标 P3。

### 15.2 其余 Juice
- 受击小顿帧（把现有 `hitStop` 扩展到"暴击/处决/碎冰"）
- 暴击飘字、连杀特写、死亡灵魂粒子上升（呼应沙雕基调）
- 指令释放全屏边缘染色（落雷=金 / 冰封=蓝 / 火墙=橙）—— 复用 `drawVignette` (L2250)
- 状态图标（头顶 3 个小方块，最多显示 3 个状态）

---

## 16. 优先级路线图（重排）

| 级别 | 机制 | 理由 | 预估改动 |
|---|---|---|---|
| **P0-a** | §1 状态系统（6 状态 + 单遍结算） | 所有后续内容的地基 | ~120 行 |
| **P0-b** | §2 炸药桶 + 补给箱（复用 `aoeAt`） | 与落雷联动最爽，玩家第一眼就感到"丰富了" | ~90 行 |
| **P0-c** | §3 指挥点 CP（先不做连锁） | 把"按 CD 就放"变成资源管理 | ~50 行 |
| **P1-a** | §1.3 状态反应表 | 花 80 行换 3 倍指挥深度，性价比之王 | ~80 行 |
| **P1-b** | §4 指令扩到 8（先上 冰封/火墙/冲锋号/诱饵） | 直接复用现有 `warn→beam`/`effect` 通道 | ~150 行 |
| **P1-c** | §4.2 敌方指挥官 AI | **不加就是破坏对称**，必须跟着 4.1 一起上 | ~70 行 |
| **P1-d** | §3.2 指令连锁 Combo | 有了 8 指令才有排顺序的意义 | ~40 行 |
| **P2-a** | §9 天候时间轴 | 每轮体验不重样 | ~110 行 |
| **P2-b** | §7 老兵晋升 | 演出价值高、实现简单 | ~60 行 |
| **P2-c** | §6 战前布阵 | 只改 `spawnArmy` 坐标，成本极低 | ~70 行 |
| **P2-d** | §5 特殊编制（先上医师/爆破手/法师） | 内容量铺开 | ~200 行 |
| **P2-e** | §8 士气系统 | 砍垃圾时间 + 造翻盘 | ~60 行 |
| **P3-a** | §15 名场面导演 | 沙雕基调的灵魂 | ~90 行 |
| **P3-b** | §10 占点与超时判定 | 改判定逻辑，风险相对高 | ~80 行 |
| **P3-c** | §13 Meta 天赋树 | 长线粘性 | ~150 行 |
| **P3-d** | §11 指挥官上场 / §12 羁绊 / §14 新模式 | 锦上添花 | — |

---

## 17. 风险与回滚开关

```js
const FLAGS = {
  status:    true,   // §1 状态系统（关掉后所有状态调用变为 no-op）
  reaction:  true,   // §1.3 状态反应表
  props:     true,   // §2 战场物件
  cp:        true,   // §3 指挥点（关掉 = 无限 CP，退回 v1 行为）
  combo:     true,   // §3.2 指令连锁
  enemyCmd:  true,   // §4.2 敌方指挥官
  specials:  false,  // §5 特殊编制
  formation: false,  // §6 布阵
  veteran:   false,  // §7 老兵晋升
  morale:    false,  // §8 士气
  weather:   false,  // §9 天候
  capture:   false,  // §10 占点
  commander: false,  // §11 指挥官上场
  director:  true,   // §15 名场面导演
};
```

| 风险 | 表现 | 兜底 |
|---|---|---|
| 状态叠加刷屏 | 满屏描边看不清单位 | 头顶最多显示 3 个图标；描边取"最强"那个 |
| 反应表连锁爆炸 | 一次点击引发 5 层反应 → 卡顿 | 反应有 0.15s 内部 CD + 单帧最多触发 2 次 |
| CP 太紧/太松 | 玩家舍不得放技能 / 无限放 | `CONFIG.cpRegen` 单点调参；先按 0.28/s 试 |
| 敌方指挥官太强 | 玩家被打崩 | AI 决策间隔 1.5s（不做 0 帧反应）+ 不用"最优点"只用"够好的点" |
| 天候 revert 漏还原 | 沙暴过后远程射程永久 −25% | 强制 `{apply, revert}` 成对书写；轮转时先 revert 上一个 |
| 特殊编制破坏平衡 | 鼓手成必选 | 名额上限 2，且鼓手 HP 只有远程的 70% |
| 性能 | 物件/状态检测每帧 O(n·m) | 全部走 0.25s 节流 + `compact` 原地压缩，粒子池上限 420 保持 |

---

## 附录：代码落点索引（按文件行号，来自当前 `脑洞军团大乱斗.html`）

| 位置 | 行 | v2 要动的地方 |
|---|---|---|
| `CONFIG` | 370 | 加 CP / 状态 / 天候相关常数 |
| `COMMANDS` | 401 | 2 条 → 8 条，加 `sys` + `cp` |
| `COMMANDER` | 407 | §11 改为真实单位 |
| `castCmd` | 413 | 加 `casterSide`（敌我共用）、CP 扣费、连锁记录 |
| `UNIT_DEFS` | 453 | 保持 3 系不动（原则 6） |
| `BRANCHES` | 548 | 可加第 4 条分支（如近战→"狂战士"） |
| `PERKS` | 614 | 加 2~3 张与新机制联动的卡（如"反应大师：状态反应伤害 +40%"） |
| `resetGame` | 650 | 加 `props / cp / weather / morale / director / cam` |
| `makeUnit` | 691 | 加 8 个状态字段 + `kills` + `spec` + 光环计时器 |
| `spawnArmy` | 713 | §6 阵型坐标分布 |
| `findNearest` | 754 | 诱饵嘲讽权重 |
| `damageUnit` | 784 | 护盾吸收 / 破盾 / 处决 / 士气系数 |
| `finishKill` | 802 | `onDeath` 钩子（爆破手）、晋升计数 |
| `attack` | 863 | 编制 `onAttack` 钩子 |
| `triggerSkill` | 893 | 大招挂状态（给反应表喂料） |
| `aoeAt` | 965 | 命中物件 → 引爆桶/箱 |
| `update` | 975 | 状态单遍结算、物件节流检测、CP 回复、天候、士气、AI 指挥官 |
| `updateEffect` | 1146 | 火墙等持久 `effect` 类型 |
| `endBattle` | 1199 | §10 超时判定改为先比占点 |
| `aiUpgrade` / `aiDrawPerk` | 1213 / 1243 | 保持镜像对称逻辑 |
| `startRound` | 1255 | `rollProps()` / 天气初始化 / 阵型应用 |
| `showUpgrade` | 1302 | 加"阵型选择"与"特殊编制"两行 |
| `draw` | 1466 | props 加入 y 排序 |
| `drawGround` | 1507 | 泥沼/治疗泉/控制点贴花 |
| `drawUnit` | 1579 | 状态描边、晋升外观、护盾环 |
| `drawEffect` | 2162 | 火墙带、冰壳、毒云 |
| `updateHud` | 2286 | CP 条、天候提示、占点进度 |
| `buildCmdBar` | 2404 | 8 按钮布局 |
| `frame` | 2455 | 复用 `timeScale` 做慢动作 |

---

### 一句话总结（v2）

先落地 **「状态系统 + 状态反应表 + 炸药桶 + 指挥点」** 这四件套 ——
它们各自只要几十行，但合起来会把游戏从"点两个技能看兵互殴"，
变成"读战场状态 → 排技能顺序 → 引爆连锁 → 出名场面"的循环。
其余 11 个机制按 §16 路线图滚动补齐，每上一个都跑一遍 §0.1 对称自检清单。
