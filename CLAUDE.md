# CLAUDE.md — AI 协作记忆

> 这是给 AI 协作伙伴（以及新加入的人）的项目共同记忆。接手任何任务前先读这里，避免踩坑、保持一致。
> 结构/依赖地图见 [STRUCTURE.md](./STRUCTURE.md)，本项目概述见 [README.md](./README.md)。

## 项目是什么

**脑洞军团大乱斗**：开罗（Kairosoft）风自动对战游戏，单文件 HTML，双模式：

- **守城远征（主模式，25-35 分钟）**：波次防守 + 金融运营。守住三段城墙挡 20 波敌军，赚金币 → 买兵 / 升科技 / 修城墙 / 建经济，钱怎么花决定守到第几波。
- **竞技场（快速局，6-8 分钟）**：经典 5 轮对称团战 + 每轮升级抉择 + Lv3 分支进化（大盾步兵 / 狙击手 / 枪骑兵）。

## 硬性约定（违反会被打回）

1. **只改 `src/`，不直接改 `脑洞军团大乱斗.html`**。HTML 是 `build.js` 的产物，改了会被覆盖。
2. **改数值必须带 rationale**。config.js 里每个数字都有解释注释；新数字要么有理由，要么标 `[PLACEHOLDER]`。
3. **改完必须跑 build + 测试**：`node build.js`，再跑 `node tests/*.js`，全绿才算完。
4. **依赖只能向下**：后面的文件可以引用前面的，禁止反向。顺序见 build.js 的 `JS_ORDER`。
5. **JS 共享全局作用域**：`G` / `CONFIG` / `$` / `PROJ` 等都是全局，没有 import/export。加新全局变量时想清楚放哪一层（见 STRUCTURE.md 分层表）。

## 数值红线（别碰坏平衡）

- **克制环**：近战→骑兵→远程→近战（+35%）；分支反转 ×2.6 / 专精 ×2.4。
- **经济五条产金路径**：税收 / 击杀 / 利息 / 金矿 / 市场。每一条都有对应 sink——**别加没有 sink 的 source**，通胀只是时间问题。
- **难度主轴只有一个**：`WAVE.budgetGrowth`。其余旋钮（税率 / 兵价 / 城墙成本）都围绕它配平。

## 已知坑（务必绕开）

- **拆/移代码必须程序化字节级切片，禁止手抄。** 之前手动把代码拆进多个文件时，中文字符串里的引号被改坏（`"`→`'`）导致 SyntaxError。正确做法：写脚本按行号区间切片，保证字节一致。
- **render.js 最大**（~915 行），别硬拆，等重构「投影/地面/精灵」三层时再动。
- **守城是「实时」模式**（相位恒为 battle），竞技场是「回合」模式。两套流程在 systems.js / update.js 里交织，改一处要两头测。
- **`desc` 文本里的换行**：卡片描述用 `\n` 分行（走 textContent），不要塞 `<br>`，否则会以字面文本显示。

## 测试

| 脚本 | 验证什么 |
|---|---|
| tests/ui_test.js | 守城底部经营条（显隐 / 页签 / 买兵 / 快捷键） |
| tests/verify_tweaks.js | 数值调参（近战锁定墙边敌等） |
| tests/verify_r14.js | 渲染 / 高分屏缩放 |
| tests/verify_merge.js | 双模式融合（分支仅竞技场、经济不串） |
| tests/probe_buy.js | 买兵门控长跑（相位不拦截） |

测试台在 `tests/headless.js`，用 `node:vm` 跑游戏脚本 + DOM 桩。加测试优先复用它的桩，别重造。

## 交付

```bash
node build.js          # → 单文件 脑洞军团大乱斗.html
```

产物可直接分发 / 打包 EXE。README 里的「单文件分享」这条路径始终要保得住。
